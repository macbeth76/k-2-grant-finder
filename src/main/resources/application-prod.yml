micronaut:
  server:
    port: 8080

datasources:
  default:
    url: jdbc:h2:file:/data/grantfinder;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password: ""
    schema-generate: NONE
    dialect: H2

flyway:
  datasources:
    default:
      enabled: true
